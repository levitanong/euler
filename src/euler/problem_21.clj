(ns euler.problem-21
  (:gen-class)
  (:require [clojure.math.numeric-tower :as math]))

(defn calcDivisor
  [n divisor]
  (range 1 ))

(defn d
  "The sum of proper divisors of n"
  [n]
  ())

(defn main
  "Amicable Numbers"
  []
  (math/sqrt 4))

(main)
